// Motion kit — the shared entrance/interaction primitives every landing section
// composes from. Built on framer-motion (already a dependency), reduced-motion
// aware, and deliberately small.

export { Reveal } from "./Reveal";
export { WordReveal } from "./WordReveal";
export { Marquee } from "./Marquee";
export { Magnetic } from "./Magnetic";
export { EASE, EASE_INOUT, riseVariants, staggerContainer } from "./variants";
